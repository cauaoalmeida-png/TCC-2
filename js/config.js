/* ============================================
   CONFIG.JS — Configuração central do FactoryTrack
   Troque apenas o valor abaixo quando o back-end
   estiver hospedado (Render, Railway, etc.).
   Todos os outros arquivos usam essa constante,
   então não é preciso alterar mais nada.
   ============================================ */

// Enquanto estiver testando no seu computador, deixe assim:
const API_BASE_URL = 'http://localhost:3000';

// Quando publicar o back-end (ex: Render), troque para a URL pública, ex:
// const API_BASE_URL = 'https://factorytrack-api.onrender.com';
